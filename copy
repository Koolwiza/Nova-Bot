const Discord = require('discord.js')
const colors = require('../colors.json')
const client = require('../index.js')
const db = require('quick.db')
const ms = require('ms')

module.exports = {
    name: '',
    description: '',
    usage: '',
    category: '',
    guildOnly: true,
    async execute(message, args){
        // This file is just for you to copy and create new commands each time. So it becomes more efficient. If theres an easier way, let me know
    }
}

//todo
//do not use copy paste const items they do not work out for you and give you a bunch of useless code.
